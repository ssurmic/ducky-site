# Research Brief 阶段 A 交付 · 2026-09-09

> 以下保留最初本地交付时的验收快照。用户随后明确授权发布，并追加显著入口、方向颜色和价格对照；当前进展与生效的开关/回滚方式见 [发布记录](RELEASE.md)。

已完成方案 review、requirements 拆解、独立只读研究页、手机与桌面适配，以及本地验收。
结果停在发布审查：**默认关闭，未推送、未合并、未部署。**

## 按 MD 落地的内容

- `00_START_HERE.md` 用于确认阶段和入口；`01_PRODUCT_PLAN.md` 转成 18 项可验收要求；
  `02_AGENT_PROMPT.md` 转成 G00–G09 执行记录。三份原文件保持原样。
  未提供的 03/04/05 及参考图没有被假装补齐；本地清单明确标为重建版本。
- 公共首页现有 A/B/C（focus / flow / brief）保留。新增独立的登录后 Research Brief，
  使用现有鸭子品牌、明暗主题和导航；没有替换旧工作台或改变免费访问规则。
- 关注/全部范围、股票与作者查询、已加载结果内的日期/文字筛选、前三条观点、
  分页与失败重试、原文链接和精确偏移、来源详情与相关历史入口均已实现。
- 原始条件、看空内容、真实零值与未知状态保持；版本时钟分开。讨论概览只计算当前结果，
  列表和数量钻取共用同一筛选，不解释为情绪或观点获证实。
- 页面复用 `/kol/research?limit=100` 与 `/watchlist`。没有每卡请求、定时刷新、模型调用、
  新依赖、采集器、通知、数据库或后端代码变化。只读数据不落入本地持久缓存。

## Review 后修复

独立数据与回归审查发现并修复：概览钻取导致分页 cursor 范围不匹配；同源兄弟观点修改导致
重复计数；旧序列化摘要显示为 JSON；持仓行为被当成普通方向；返回页数/阅读状态及翻页
键盘焦点丢失。最终 23 项新增测试覆盖这些问题及用户切换、晚到响应、输入转义与默认关闭。

手机首屏压缩了说明与筛选占用，内容出现在约 335–345px；桌面采用记录与概览双栏。
原始内容通过逐层展开保留，主操作按钮适合点击，减少动效偏好有对应样式。

## 已验证

| 验收 | 结果 |
|---|---|
| 旧版前端基线 | 549 passed |
| 最终前端构建及测试 | 572 passed，0 failed |
| 后端冻结基线 selftest | 4,634 passed / 6 skipped，ALL GREEN；本轮没有后端代码变化 |
| 文案 / 内部链接 | 3,034 文件 / 1,290 链接通过 |
| Python 资产检查 | 7 passed / 1 个既有历史版本检查 skipped；公开数据清理额外 1 passed |
| 响应式 | 320、390、1280宽 × 中英 × 明暗，12组无脚本错误/横向溢出 |
| 默认关闭与旧路径 | 实际 config false；新导航/CSS不加载；ABC保留；旧自选页仅1个原有业务请求 |
| 100条研究 | 2个GET，初始20张卡片，无随卡片数增加的远端请求 |
| 真实留存适配 | 9月8日旧 `/1` 的2条研究/4条观点，中英内容/方向/缺失值保真；SQLite写入0 |

完整证据：[独立回归](REGRESSION_FINAL.md)、[浏览器与五次计时](BROWSER_ACCEPTANCE.md)、
[真实留存记录与限制](RETAINED_DATA.md)、[需求](REQUIREMENTS.md)、[执行清单](EXECPLAN.md)。
旧页资源传输增加0.024%，五次资源耗时之和的中位数增加5.2%；首轮冷加载慢样本增加12.5%，
已调查并保留。该数字不是页面延迟或线上性能承诺。

## 本地查看与回滚

代码：`/Users/zizhaozhang/dev/ducky-site-research-brief-20260909`，分支
`codex/research-brief-20260909`，基线 `4a88b28`。改动保留为可审查的未提交文件。
隔离后端分支仅更新 `DUCKY-IMPROVEMENTS.md` 的本地交付记录。

当前可查看 [中文样例预览](http://127.0.0.1:8920/qa-frame?lang=zh&theme=light&route=research-brief)。
此地址只在本机有效，使用明确标记的合成记录；不连接生产账户或行情。

```bash
cd /Users/zizhaozhang/dev/ducky-site-research-brief-20260909
/Users/zizhaozhang/dev/ducky-bot/.venv/bin/python build.py --research-brief-preview
python3 scripts/research_brief_qa.py --port 8920
```

正常构建不传 `--research-brief-preview` 即默认关闭；独立终审已经实际构建并验证。
撤销预览只需省略该参数重新构建，原有 route/nav 行为恢复；没有数据迁移需要回滚。
构建器原本会刷新 `public/calendar.json`，本轮已还原这项无关生成变化。

## 发布前仍需验证的边界

- 留存真实样本是旧 `/1`，未伪装为当前 `/3`；真实样本的条件/摘录原本为空，非空条件与
  摘录的保真由合成回归覆盖。历史 STHO 关联不能证明现行有效性。
- 当前生产 `/3`、真实账户权限、真实来源/历史页面解析、部署环境性能和实体 iOS/Android
  尚未做现场验收。已有后台契约测试通过，但不能替代真实上线验收。
- 新视图的读取不产生新业务写入；现有完整 HTTP 认证在冷启动时可能初始化主数据库，
  tiered creator 访问也有既有初始化路径。不能把纯只读 research 函数等同于整个 HTTP 栈零写入。
- 只覆盖现有已保存研究，不代表全部视频、提及或完整历史均已采集。原有博主页仍可访问。
- 原前端工作区的 tracked diff 与开始时完全一致。原后端脏工作区的 diff 在期间变化，
  终检已记录；本轮实现和跟踪记录只写隔离目录，没有覆盖或还原原目录的变化。

三个 Agent 分工：主 Agent 负责实现/浏览器/交付，数据 Agent 负责语义和真实留存审查，
回归 Agent 在独立临时快照完成测试及默认关闭检查。没有执行生产写入或发送消息。
