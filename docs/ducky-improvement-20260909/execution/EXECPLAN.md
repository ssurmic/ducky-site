# Research Brief execution plan

Goal: reviewed, working Stage A preview in an isolated local environment; stop before release.
The supplied prose, repository rules and REVIEW.md define the scope. Companion task/acceptance
files were absent; this checklist is reconstructed, not copied from a missing attachment.

## Progress

- [x] G00 repository, existing A/B/C, flags, authorization and read-path audit.
- [x] G01 freeze baseline tests/build/browser and prior failures.
- [x] G02 pure read adapter, identities, timestamps, counts and adversarial contracts.
- [x] G03 build-default-off preview shell, route/login integration and CSS isolation.
- [x] G04 useful Brief: bounded current research, watched/all scopes and honest coverage.
- [x] G05 original/source-history/stock links, reversible detail and navigation restoration.
- [x] G06 creator/ticker server filters, loaded-result date/text filters, pagination/race handling.
- [x] G07 conditional overview: stable-ID counts sharing the same selector, max 20 rows.
- [x] G08 desktop/320/390, EN/ZH, light/dark, keyboard and bounded performance.
- [x] G09 complete regression, off-switch/rollback, actual-data evidence and final report.

## Discoveries / decisions

See REVIEW.md and REPO_MAP.md. Use current mainline isolated branches, not the September 5 dirty
checkout. Initial requests: at most one bounded study page and one private watchlist read.
No per-card API fetch, no automatic extra pages, no summaries generated by the preview.
Keep UI data in route memory; preserve filters and scroll for back navigation without placing
private watchlists, arbitrary search text or source text in the URL or shared persistent caches.

## Acceptance evidence to collect

Commands/exit codes; old unit and contract suites; build/lint/link checks; before/after browser
screenshots; input escaping; original offset zero/unknown; missing clocks; duplicate/conflicting
identities; unknown stance; losses/original text unchanged; paging/partial errors/no matches;
auth/free/pro/user switch; default-off import/request/CSS absence; old route request comparison
over five repetitions; 100-row constant remote-request count; rollback rebuild.

The required real retained-data check must be recorded separately from synthetic UI tests.
Physical iOS/Android, production release, notifications and external account writes are outside
this local acceptance and cannot be claimed. Do not mark unexecuted checks green.

## Commands and outcomes

Baseline frontend: `DUCKY_TEST_PYTHON=/Users/zizhaozhang/dev/ducky-bot/.venv/bin/python npm test`.
Baseline backend: `/Users/zizhaozhang/dev/ducky-bot/.venv/bin/python bin/selftest.py` (no launcher,
no `.env`, no `--notify` or network firehose flags). Logs are adjacent; frontend baseline 549 passed; backend baseline 4,634 passed / 6 skipped, ALL GREEN.

Completed locally. See FINAL_REPORT.md, BROWSER_ACCEPTANCE.md, REGRESSION_FINAL.md and RETAINED_DATA.md. No release was performed.
