{
  "name": "ducky-site",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "${DUCKY_TEST_PYTHON:-python3} build.py && node --test tests/*.test.js"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
