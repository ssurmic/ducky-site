# Related evidence cards · 2026-09-13 PDT

Status: implemented; not deployed.

The owner's same-video ORCL example now shows one green card and “2 related points”. Expansion retains both statements and their separate source routes. Existing graph payloads and historical snapshots use the same source folder fallback; no extraction or model call is needed for those folders. Optional reviewed semantic groups preserve every constituent point. Different stable creators, directions, actions, explicit conditions/horizons and generic mentions stay separate. Grouping occurs before preview pagination; counts remain original records, not independent votes.

Shared backend ownership, incremental jobs, source validation and activation limits are documented in the private backend `reports/EVIDENCE-FOLDING-2026-09-13.md` and SYSTEMDESIGN. Cross-video inference is opt-in; this frontend does not initiate it.

Synthetic browser acceptance (Codex in-app Chromium): Chinese/English × dark/light × 320/390/393×650 and 1440×900, 16 combinations. Native disclosure is initially closed, keyboard Enter opens it, both original children remain, and document overflow is false. Phone folder 212px, expanded 408–433px; collapse header 58px. Phone app header/navigation leave 522px of the 650px test viewport. Dark Chinese phone and light English desktop visually inspected. Second child retains its own creator/stock/point route and 10:34 source position. No physical iPhone or Safari check claimed.

Unit acceptance covers pre-pagination grouping, unchanged record counts, opposite views, missing/malformed group metadata, author identity collisions, conditions, historical/current inputs, source withdrawal, preserved expansion, exact child links and no fetch during presentation. Tour target resolution traverses closed folders without reporting that a source was read.

Final local gates: 756 JavaScript tests passed; 22 Python release/export tests run, 1 skipped; 30 pages built; copy lint passed and 2133 internal links checked. Exact source details are retained in the pull request. Production data, pricing and app navigation are unchanged. Rollback: revert this change; old raw records and immutable source references need no migration.

The final phone source-dialog check resolved the second synthetic video point to `t=634` (10:34), kept the ORCL/creator/point route, and returned focus to `ORCL:node:orcl-attention` with its group still expanded after Escape. Final app module graph: `6e8061fbd3f37afe3b11` before the final metadata guard rebuild; the committed build is reproduced in CI.
