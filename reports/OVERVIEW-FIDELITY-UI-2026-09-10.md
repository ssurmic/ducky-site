# 完整概览的手机阅读验收

状态：2026-09-10 本地浏览器与回归通过；本次只增加验收夹具和测试，没有更改公共应用代码。

后端正在调整双语概览的长度分配，保留作者、日期、因果机制与限定条件。
前端继续使用已有列表折叠/展开：首屏短预览，展开后显示完整已保存文字和引用，
点击不生成摘要或获取新的来源。

`case=fidelity-overview` 是 loopback QA 专用的合成记录，不连接生产、不进入部署资源。
它包含长英文作者名、完整日期、条件和反面观点。此验收只证明布局与交互，
不证明这些示例是已审核的股票观点，也不代表真实摘要覆盖率。

## 实际浏览器检查

2026-09-10 18:58–19:00 UTC，使用本地 `/qa-frame`，检查：

| 语言 / 主题 | 视口宽度 | 结果 |
|---|---:|---|
| 中文 / 浅色 | 390px | 列表默认、完整展开、引用打开原文对话框 |
| 英文 / 浅色 | 320px | 长作者名和完整条件换行，正文可滚动读完 |
| 英文 / 深色 | 390px | 展开文字、日期和原文入口可读 |
| 中文 / 深色 | 320px | 展开文字完整，列表保持固定股票/导图列 |

四次 `documentElement.scrollWidth` 均等于视口宽度；指标表本身保留横向滚动。
手机窄屏的完整段落需要纵向滚动，不能把它限制到两行后丢失条件。
最后恢复浏览器原尺寸。

`node --test tests/product-focus.test.js`：22 passed。新增测试核对完整英文
概览字符串与原始引用 URL，点击引用时 fetch 次数为零。生产界面继续使用
既有 `680086e7` / Pages `31db4766`，无需因测试文件另发一次 Pages。
