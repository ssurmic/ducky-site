# 作者历史第一页的关注范围 · 2026-09-10

状态：后端 `175a7675`、前端 `92026a55` 已发布，并在正式登录页面完成中英文复验。来源是浏览器中真实重现的问题。

用户从左栏 Creators 点 Call history 后看到 “No matches in this batch”，需要再点 Load more。
原因是 Pro/open 模式的 `/kol/research` 默认返回全库第一页，前端再用已关注作者过滤。
“当前这批没有匹配”虽然没有谎称全库为空，但让已存在的历史记录不可见，多加了一步。

## 同数据对照

13:36 UTC 按正式浏览器选择器显示的同一组 10 位作者，只读比较原有 feed 查询：
全库第一页 50 项，筛选后 0 条观点；先限定作者再分页，第一页返回 47 项、包含 22 条调用记录中的观点。
22 是返回 calls 数，尚未经界面去重、分组及状态筛选，不计作 22 条已交付独立观点。
原发表时间与分页游标保留，无模型调用或生产写入。

## 修复

API 增加可选 `following=true`，从已认证账号读取关注关系，与当前可访问作者范围取交集，
再交给原 `kol.research_feed(allowed_creators=...)`。既有 SQL 在分页前限定作者，游标绑定这个范围。
没有新表、采集、价格计算或推理；空关注返回空结果，不偷偷回退全库；Discover 和个别未关注作者仍可正常访问。

前端 Call history 的 Following 视图及后续翻页都传同一标志，保留本地范围复核。Discover 不传这个限制。
关注/权限改变导致旧游标失效时，保留可读当前页并让 Retry 从第一页开始，避免一直重试同一无效游标。
没有自动遍历全库、扩大分页上限、修改关注关系或伪造历史。

## 测试与发布

4 项新增后端集成测试覆盖真实存储中的第一页过滤、续页、空关注、默认全库、认证账号、权限交集和股票筛选；
新增 3 项前端测试覆盖真实页面导航接线、续页保留过滤、失效游标恢复。前端完整 618 项通过。
加入冷启动只读补丁后的最终后端 selftest：4,827 passed / 6 skipped / 19 warnings，90.72 秒，ALL GREEN；架构检查 0/0。前端 copy lint 3,289 个文件无问题、1,274 个内链通过。

此修复使已存在的作者历史更容易找到，不证明作者内容全部收录、收益正确或目前所有日期/语义均已通过人工验收。

发布前只读审查补充：关注关系读取使用只读数据库连接；冷启动不创建文件、不迁移 schema。新增冷数据库、已有订阅稳定排序、免费/Pro API 入口回归。生产 NVDA 在部署前已由新来源产生新的分析任务，旧错误摘要的拦截与当前任务完成要分别验收。

最终冷启动补丁：9 项聚焦后端测试通过；后端 PR #21/#22 及最终精确 web CI [34485960664](https://github.com/ssurmic/ducky-bot/actions/runs/34485960664) 通过。前端 [PR #13](https://github.com/ssurmic/ducky-site/pull/13) 及精确 main CI [34484377137](https://github.com/ssurmic/ducky-site/actions/runs/34484377137) 通过。

## 正式发布和实际页面

- 后端 `175a76754feb7e5c03a9b4e71e4a528d473a451c` 通过原发布门禁、等待在途工作结束后切换。API 于 14:07:53 UTC 启动；14:09 健康检查、运行 SHA 与原分析、证据、行情 timers 均正常，配置变更为空。恢复包 `recovery-20260910T140718373046Z-71d56c66`；上一运行版本 `9a6aebfd8421ca39b79a98b4552462d9f9e2de99`。
- 前端 `92026a552759f1c1d15c0304beef80020904491a`，Cloudflare Pages `7a25997b`，模块图 `1fd559894eca2fda7830`。14:10 UTC 正式域名的配置、双语入口、两份 creator 模块和 CSS 六项哈希均匹配构建产物。前端回滚版本 `533cd73d` / Pages `ce3835e3`。
- 正式英文和中文页面的 Following → Call history 首屏均显示 **16 条观点、8 个作者与股票组**；这些是前端筛选和去重后的实际可见计数，与查询阶段的 22 条 calls 分开。无需点 Load more 才能看见内容。此页 16 条均未满 20 个交易日，完成对比为 0，未包装为完整作者胜率。
- 展开投资 TALK 君的 AVGO 组，实际呈现 3 条独立记录、各自方向、9 月 3 日发表时间、9 月 2 日参考收盘与 9 月 9 日已记录收盘。原文详情保留 9 月 10 日修订时间及视频 16:13 链接，缺少 first-collected 时显示缺项，未补造日期。
- 中文自选股仍为列表第一且默认、概览第二，表头为排序按钮，43 行均保留直接导图。NVDA 无可读摘要时，一次点击仍进入 123 条记录的三栏导图，保留作者归组和同原文重复提示。此处是桌面正式浏览器；早前的 320/390px 验收是模拟视口，不能称手机实机。
- 首次发布后英文新文档曾出现一次通用页面加载失败，点击原刷新按钮恢复。后续独立英文新文档加载、中文语言切换和股票/导图导航正常；不推断未经证实的失败原因，也不把一次恢复当可靠性 SLA。

公开资产凭证见 [六项校验](product-focus-20260910/creator-history-site-verification.json)。

## 内容验收仍未通过的事项

首屏仍暴露旧摘要质量问题：ORCL 英文含未翻译的“热度”；AMZN 的“3510”缺单位且可能有主语问题；COIN 关联的片段只解释 ZEC，股票关联须核原文。AVGO 展开详情重复显示同一长摘要。它们分别是翻译、证据含义/实体归属和渲染重复问题，不能通过扩大页数、删除原始记录或临时前端改写文本解决。此次分页发布没有宣称修复这些内容。

14:12 UTC 十股只读审计为 **8/10 可读**，均保留各自日期；NVDA 和 MU 未就绪。NVDA 的新来源任务正在 building，旧日期错误文本未投影；不能将它描述为已经重新审核完成。分钟级价格、连续交易日 SLO 与真实用户研究仍待验收。
