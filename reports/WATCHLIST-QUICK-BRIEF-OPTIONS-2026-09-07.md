# 自选股短简评、期权期限与K线 · 2026-09-07

本地实现，尚未发布。接续 FULLSTACK-AUDIT-2026-09-07；使用独立工作树，保留原目录改动。

## 完成的界面

- 列表每只股票旁增加AI简评按钮；热力图选中详情和完整图表也有入口。
  弹窗先展示已有的经复核短简评，再展示K线，完整条件、风险和引用按需展开。
  中文起草100字以内，服务端发布校验上限130字，不截断句子；英文独立表达。
- 点击只读一个既有共享报告，不触发推理。仅选中股票加载图表，Free不请求私有简评。
  关闭中断请求、清理订阅/图表/定时器并恢复焦点。旧来源修订、失败与待生成分别提示。
- 价格历史与期权到期日独立选择；明确YYYY-MM-DD与本周/下周/本月，完整剩余天数和
  采集时间在图后保留。到期日切换不重拉K线，历史切换不改变所选到期日。
- K线默认3个月，安静网格、清楚的收盘价、成交量和OHLC。RSI/MACD及额外区间按需打开。
  远处墙不再压扁蜡烛；未知成交量不画成真实零值。
- 逐到期日Call/Put、OI、Max Pain和估算区间来自 `option-context/1`。
  旧合并范围在研究记录明确标注，股票详情也改用同一单到期日资料，旧错误范围不沿用。

## 修复的交互回归

320px时AI按钮挤压日涨跌列：新增自适应列宽，主按钮内容不溢出、不嵌套按钮。
首次网络加载有股票时收起未被用户操作的添加表单。手机弹窗去掉重复关闭栏，
主关闭/控件维持44px；输入仍16px。慢历史请求不再污染后来展开指标所复用的缓存。

## 验收证据

本地Chrome临时夹具使用真实构建模块和实际取得的BE/NVDA/NOK资料，仅认证状态为LOCAL QA。
BE期权采集时间2026-09-07 23:17:41 UTC：9/11 Call275、Put230；9/18 Call260、Put230。
9/18样本缺部分IV，页面明确不完整。原始输入未进入Git或公开dist。

- 320×650英文浅色：列表首屏约2行，主股票按钮scrollWidth等于clientWidth；AI、价格、涨跌分列。
  来源修订状态的弹窗K线从465px开始，至637px底部约172px可见；完整到期日标签未截断。
- 390×650中文深色：弹窗K线从441px开始，约196px首屏可见，切9/18正确显示260/230。
- 1280×900桌面英文浅色：6个月K线+9/18期权选择保留独立，只有一个图表；指标展开正常。
- 日期变化/过期/未来时间、Free私有请求、关闭取消/焦点、无额外抓取与异步竞争均有回归。
  没有声称实体手机、Safari、实际支付或通知送达验收。

最终测试结果见下方回执。
该次真实模型试跑的旧报告因期限错误被撤下，新起草出现超时；不以失败稿充当复核完成。
后台完整证据、数据口径、发布顺序见ducky-bot同名报告。

## 发布要求

先发布后台逐到期日投影，再等共享采集与简评完成，最后核对正式Pro的弹窗和图表。
旧快照没有逐到期日字段时显示未知，不能用旧expected.expiry猜测墙的期限。
不改选股、同行分类、阈值、分数或交易规则。此前X功能保持关闭。


最终代码门禁：后端全量1,995 passed、5条依赖警告、selftest ALL GREEN；末次引用标号空格
清理后32项简评/期权专项通过，架构0/0。前端全量328 passed、无失败/跳过，构建20页；
移除全部LOCAL QA夹具后，文案2,118文件与1,306内部链接检查通过。资产/公开边界专项
5 passed、1项既有skip。Git不包含环境文件、运行数据、会员夹具或构建生成的日历时间改动。

最终真实模型状态：新提示词两次起草均在300秒预算内未完成，记录为LocalModelTimeout；
对已捕获模型草稿提供明确日期/13F口径纠正后的修稿，在240秒共享锁预算内未获可用模型，
记录为repair_failed_LocalModelBusy。模型服务健康读取可达，生产已有10份其他共享报告；
这不证明本轮BE新简评已完成。未调整共享模型配置、抢占其他任务或绕过事实复核。
**本轮新简评的真实起草→复核成功验收仍未完成**；需要模型资源可用后再按同一证据链验证。
界面与数据修复已完成，代码尚未推送/部署；不能将本地测试或已有其他报告当作新版上线结果。


## ORCL实体手机截图跟进（2026-09-07，本地修改）

老板追加的截图仍为未发布新改动前的界面。按同一ORCL场景检查后，继续压缩独立图表页：
公司名与折叠的股票切换共用一行；日线收盘标签保留日期，完整报价说明在title；手机历史
选择器与标签同排，OHLC明细移图后。输入16px、主要按钮44px和期权日期完整可见。

与本轮初版的独立图表页相比，393×700中文深色下，K线起点从435.46px变为330.53px；底栏631px，首屏有效K线区域
从约195.5px增加到300.5px。320×650英文浅色起点同为330.53px、底栏581px，当前价
附近走势可见；展开股票搜索不溢出。1024×800桌面通过。模拟窗口改变还暴露默认图表
保留像素间距、拉宽后左侧留白：启用可见范围随尺寸保持，并允许两年日线在窄屏完整
fit，避免最小蜡烛间距把所选长区间裁短。ORCL六个月及BE两年跨320→1024尺寸实看通过。

ORCL额外真实采集为2026-09-07 23:54:26 UTC，参考股价158.78：9/11墙165/140、9/18墙
160/150；选择9/18再切六个月K线，仍明确显示该到期日与160/150。9/18缺部分IV仍标不完整。
本次只读取共享价格/公司缓存并在隔离进程采集期权，不发模型请求、不写报告/用户名单，
不发送通知。改动尚未上线。UI验收是Chrome视口模拟，不是实体
Safari操作；截图本身来自老板的实体设备。

ORCL追加修改最终门禁：328项前端全量通过；资产/公开边界5 pass、1既有skip；文案2,134文件、1,306链接检查通过。临时会员夹具已清除，未提交采集数据。
