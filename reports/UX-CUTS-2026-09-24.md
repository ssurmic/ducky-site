# UX cuts: simulation, leaderboard, research brief, orphan routes, dead billing links · 2026-09-24

**Status:** implemented on `codex/ux-cuts-20260924` (from `main` `f1b63bf`), gate green locally, **in review, not deployed**.
Proposal → merged code → deployment → delivered content are separate steps; only the first is done.

## What and why

Owner decision in chat, 2026-09-24: "该砍的都砍，开始做". It reverses the 2026-09-05 approval of KOL-07/08 (creator
simulation and creator leaderboard) and removes three other pieces that nothing links to or that duplicate a kept route.
Six cuts and two bug fixes, all frontend; no backend endpoint, data model or billing logic changes.

| Cut | What went | Size |
|---|---|---|
| Simulate a view (观点模拟) | `views/creator-simulation.js`; `lab` tab, Simulate buttons (post cards, view dialog, creator page, Call-history rows), `?tab=lab&preview=fictional` route state; demo-recording scene | 101 JS lines + 25 wiring lines, 51 keys/lang, ~60 CSS rules |
| Creator leaderboard (博主排行榜) | `views/creator-leaderboard.js`; `rank` tab | 39 JS lines, 23 keys/lang, ~30 CSS rules |
| Research Brief route | `views/research-brief.js`, `research-brief-model.js`, `research-brief-prices.js` (only the view imported them), `css/research-brief.css`; route branches in `router.js`, `login-target.js`, `navigation.js`, `templates/app.html`; `research_brief_preview` flag, `RESEARCH_BRIEF_ENABLED`, `--research-brief-preview`; CI step; README paragraph; `tests/fixtures/research-brief-browser.js` | 471 JS lines, 74 CSS lines, 92 keys/lang, 32 node cases + 3 Python cases |
| Orphan routes `#/market`, `#/degen` | `views/market-context.js`; `market`/`degen` in `router.js`, `views/discovery.js`, `login-target.js`, `navigation.js`; dead `degen` branch in `social-tracking.js`; `product-navigation.json` catalogue entry + reports-tree child (drops the homepage `/app/#/market` link) | 90 JS lines, 29 `app.market.*` + `nav.market` + `nav.degen` + 3 `degen.*` + 3 `desk.*.market` keys/lang, 3 node cases |
| Dead old-menu code | non-focus `<nav>` + `tree_group` macro in `templates/app.html`; `nav-tree`/`nav-more` logic in `navigation.js` + `router.js`; CSS in `app.css`, `workspace.css`; `app.nav.more`, `app.nav.evidence_short` | 46 template lines, 28 JS lines, ~45 CSS rules |
| Dead `#/billing` links | 20 link sites in 14 modules → `#/profile` with `app.common.account` ("账户" / "Account"); two pure upsells dropped (`experience.js` freeGuide "Compare Free and Pro", quotaNote "See Pro limits"); 12 orphaned label keys removed | see list below |

Totals: 68 files, +166 / −2,164 lines. `public/js`: −800 / +47. CSS: −193 / +24. i18n: −219 / +2 keys per language
(4,645 → 4,428; the two key sets stay identical, build-enforced).

### Billing links (all were dead: `#/billing` renders Profile while billing is paused)

`onboarding.js` tour-expired card · `ui.js` lock veil + limit modal · `macro-beta.js` signed-out card · `entitlements.js`
banner · `router.js` trial-ended card · `views/updates.js` gate · `views/briefing.js` creators card · `views/watchlist-manager.js`
· `views/boards.js` access note · `views/social-tracking.js` 402 card · `views/creators.js` (2) · `views/opportunities.js`
paywall · `views/calendar.js` banner + "mine only" redirect. `templates/_partials/pricing.html` still carries a
`#/billing?currency=` link but is not included by any page (open access) and was left alone. No pricing, "Pro", "14 天" or
"升级" wording was introduced; `tests/open-access.test.js` and `lint_copy.py` keep enforcing that on public pages.

### Removed i18n keys (per language, 219)

51 `app.creatorlab.*` · 23 `app.creatorrank.*` · 92 `app.researchbrief.*` · 29 `app.market.*` used only by the market-context
view (`calendar_link counter counter_source coverage creator_attribution description ducky_inference evidence eyebrow
kind_creator_view kind_news_headline kind_prediction_market loading next_check open_research partial_sources
pending_label preview_label scope source_only summary_label ticker_basis title topic_sources unavailable watch_empty
watch_matches watch_related watch_unmatched`) · 3 `app.degen.*` · `app.nav.market app.nav.degen app.nav.more
app.nav.evidence_short` · `desk.title.market desk.access.market desk.preview_market` · `app.creatorpage.adjust
app.creators.horizon` (simulation-only) · 12 billing labels `app.experience.compare app.experience.more app.tour.pro
app.updates.upgrade app.briefing.membership app.creators.upgrade app.calendar.upgrade app.macro.open
app.radar.access_upgrade app.trial.manage app.nav.billing app.common.unlock`.

Added: `app.common.account` ("账户" / "Account"), `app.watch.signal_open_disclosures` ("查看披露记录" / "View disclosures").

### Two bug fixes

1. `public/js/app/watchlist-signals.js`: the politician-trades card action opened `#/boards?board=political&ticker=T` but
   was labelled with `radar.chart` ("查看图表" / "Open chart"). It now uses the new `watch.signal_open_disclosures` key.
   Assertion added in `tests/watchlist-signals.test.js`.
2. `public/js/app/views/research-record.js`: the `options` stream linked `#/watchlist?ticker=T`, a query the watchlist
   ignores. It now opens `#/chart/T` like the price and technical streams. Assertion added in `tests/research-record.test.js`.

## Tests

- `creator-workspace.test.js` 11 → 7 (simulation maths, earliest-verified-version selection, singular labels, free
  simulation); `creator-navigation.test.js` 6 → 5 (simulation deep links + ranking; the safe-target case now uses
  `tab=research` and checks that `lab`/`rank` fall back to the feed); `workspace-ux.test.js` 3 → 2 (compact market dialog).
- `creator-simulation-target.test.js` (3) → `creator-analyze-contract.test.js` (1): the `api.kol.analyze` contract was
  independent of the view and is kept. `market-context.test.js` (5) → `calendar-prediction.test.js` (2): the two
  `predictionPanel` cases belong to `calendar-prediction.js`, which stays.
- Deleted: `research-brief-model.test.js` (12), `research-brief-prices.test.js` (8), `research-brief-view.test.js` (12),
  `test_research_brief_config.py` (3, Python), `fixtures/research-brief-browser.js`.
- Rewritten for the single focus shell: `app.test.js` ("both app shells carry every navigation icon locally"),
  `discovery-navigation.test.js` (retired routes fall back to the default page and are not remembered through sign-in;
  no More menu in either language). Billing assertions in `briefing`, `creator-retry`, `creator-updates`, `evidence`,
  `free-experience`, `opportunities` now expect `#/profile` (or no link where the upsell was dropped).
- `tests/homepage.test.js` still asserts the homepage has no `tab=lab` link.

## Gate (local, 2026-09-24)

| Step | Result |
|---|---|
| `DUCKY_TEST_PYTHON=…/.venv/bin/python npm test` | build OK; **757 / 757 pass** (baseline on `f1b63bf`: 800 / 800) |
| `scripts/lint_copy.py` | OK — 4,145 files, 0 banned strings, 0 brand terms, disclaimer lines present |
| `scripts/check_links.py` | OK — 2,034 links checked |
| `unittest -p test_export_desk_prices.py` | OK (3) |
| `unittest -p test_build_assets.py` | OK (5, 1 skipped) |
| `test -s dist/index.html && dist/en/index.html && dist/_headers` | OK |
| `grep -rl "creatorlab\|creatorrank\|research-brief\|#/market\b\|#/degen" dist/` | only under `dist/app-assets/<old hash>/` — prior release graphs that `build.py` rebuilds from git history for already-open tabs (`publish_app_modules(retain_history=True)`); the current graph `67c5bd44…`, both app shells and every page are clean |
| `public/calendar.json` | regenerated by the build, restored with `git checkout --` before committing |

Not run: phone-browser visual sweep (no UI was added; the only visible changes are two tabs and a handful of buttons that
no longer render, and relabelled links). The reviewer should still open `#/creators`, a stock's watchlist-signals
politician card and `#/profile` on a 390 px viewport before deploy.

## Kept on purpose

- `#/vibe`, `#/macro`, `macro-beta.js`, the homepage `market-context-preview` page and `market-context-preview.js`.
- `views/billing.js` and the `#/billing` route alias (still renders Profile; `login-target.js` still sanitises old
  checkout links; `tests/open-access.test.js` covers it).
- Backend `/kol/leaderboard` (separate decision) — the site just stops calling it.
- `PRODUCT_FOCUS_ENABLED` and the `product_focus` template guard (`tests/product-focus-release.test.js` pins the coupling).
- `scripts/research_brief_qa.py` as the local fixture server; `scripts/demo/recording/stage.css` still carries
  `[data-scene=simulation]` rules and the frozen recordings `index.html` / `en.html` still mention the old keys — retired
  demo assets, not built into `dist/`.
- The `tree` in `product-navigation.json` (the homepage tool catalogue renders it); only its `market` child went.
- Files reserved for the parallel slice (`views/stock.js`, `views/chart.js`, `views/watchlist.js`, `stock-price-chart.js`,
  `views/today.js`) were not touched.

## Rollback

Revert the merge commit of `codex/ux-cuts-20260924`; baseline is `main` `f1b63bf`. Nothing is deployed and no data or
backend state changed, so no migration or cache step is needed. Re-enabling any single feature means restoring its files
from `f1b63bf` plus its i18n keys and CSS from the same commit.
