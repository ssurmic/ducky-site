# 首次摘要读取失败恢复 · 2026-09-10

状态：候选已完成本地测试和浏览器验收；发布凭证随后补充。

## 已确认的代码问题与修复范围

正式中英文切换曾出现整列摘要读取失败，手动 Refresh 后恢复。同期 API 日志200、
浏览器未记录异常不足以定位最初的错误原因，不能认定都是服务器慢或模型排队。

原自动刷新只观察成功的 GET。首个请求失败后，该路径没有自动补读机会。
现在仅 `/me/stock-research` 与 `/stock-research/:ticker` 的首次网络/超时或指定
临时服务器错误可进入已有刷新表，最多两次恢复尝试，沿原轮转/退避调度运行。
成功后恢复正常来源重验；恢复预算耗尽后保留失败状态，用户仍可手动 Refresh。
产品聚焦模式原间隔30秒，每轮一个路径；这不是每个接口30秒完成的 SLA。

隐藏/离线页暂停；离开页面、取消和账户epoch变化停止。401/402/403/429、
会话变化、非研究请求、搜索、历史游标、写入均不会因首次失败被自动重放。
没有额外的按股票请求、行情抓取或模型生成。正常成功后的权限变化/来源撤回
继续通过原服务端及前端校验执行。

原15秒超时在响应头返回时已清理；现在 JSON 响应体读取也受同一个期限约束。
原 `detail=network` 兼容字段保留，增加 timeout/cancelled 分类。raw Response
调用仍由调用者消费 body，不宣称它也获得 JSON 期限。

自选股将 API 读取/数据格式失败与 render 异常分开。后者不会清空已接受的
结果再谎报为后端未生成。内部 console 仅记录阶段、枚举原因、HTTP状态，
不记录响应原文、URL查询、账号或token；无需新增遥测服务。

## 验证

- 完整前端套件627通过；随后新增渲染故障保留结果回归，8项故障测试全通过。
  最终CI将再次运行全部628项。双语copy lint通过，1,274链接通过。
- 故障测试涵盖：首读503后原位恢复、仅两次失败补读、离线/隐藏暂停、
  权限/限流/取消/会话更换不重放、旧页晚到失败不进入新页、恢复后来源撤回、
  响应体超时、异常格式及渲染失败分类；恢复保持筛选及地图焦点。
- 本地浏览器独立合成数据 `case=recover-first-read`：首个摘要GET503，价格和
  四个地图入口立即可用，下一轮只有一次摘要GET，四份原有分析及日期原位呈现，
  没有点击Refresh、页面导航或写入。用户真实股票未修改。
- 320×650 /390×650 ×EN/ZH ×light/dark八次目检：document宽度等于viewport，
  主内容522px，首行y=335.18。320英文约2行可见，其余约2行和第三行边缘。
  宽表在自身容器内横向滚动，不声称所有指标在一个手机屏内同时可见。
- 390中文深色展开摘要→原来源弹窗→关闭：条件、原文和日期保留，焦点回到
  `NVDA:citation:first`，摘要仍展开。1440×900英文浅色YTD表头排序通过。
- 这是浏览器viewport模拟与合成错误注入，不是实体手机、真实网络故障或生产SLA。

## 尚未解决

首次生产错误的具体分类需要新诊断部署后再次观察。这个修复不启用分钟报价，
也不解决 NVDA 等股票因输入候选变化而失效的生成任务。后端型式化输入、旧
分析来源兼容和实际内容覆盖仍按独立验收推进，不能因这里测试通过宣布系统完成。
