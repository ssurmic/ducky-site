# 列表中的分析日期 · 2026-09-10

状态：本地、精确 main CI 和正式发布通过；读取稳定性和内容覆盖仍未完成。

用户在列表里看见旧摘要与价格，以为它们都等待同一队列。原列表折叠预览只复制
摘要文字，把原有日期/上一版状态藏在展开后。现在预览同时复用 `stock-reading`
的同一日期说明，直接显示“分析截至”或“上一版分析；新资料正在核对”。
没有新时钟、来源推断、请求或 LLM 调用；读取中/读取失败/未就绪仍保持其真实
状态。展开后只显示原有完整内容及日期，引用和历史证据入口保留。

手机继续使用列表默认、概览第二、表头双向排序、固定 ticker / 导图以及横向
指标滚动；320px 下把概览列限定到固定股票列之外剩余可读宽度。输入和交互
目标保留原尺寸，没有缩小触控区。

完整测试 **620 passed**；新增回归覆盖 ready、refresh_pending、read_pending、
read_failed 的折叠预览与日期、原引用保留。copy lint、1,274 internal links 通过。

隔离合成数据浏览器验收：320×650 和 390×650，EN/ZH、light/dark 共八种；
页面宽度均等于视口，主内容高522px，首个股票行顶部约335px；390px 有两个
完整行和第三行开头，320px EN 有约两个可见行，ZH 可见第三行开头。表格按
原设计内部横向滚动，不把横向数据裁剪说成全部同时可见。320px 横移80px后，
固定股票列右边127px、日期文本143–296px，完整可读；日期约153px宽、自动换行。

320px 中文暗色展开 → 查看依据 → 关闭，历史日期与条件完整保留，焦点回到原
citation，原行保持展开。1440×900 英文浅色桌面确认四行、日期和导图；控件仍
按原状态更新。以上是浏览器尺寸模拟，没有声称实体手机测试或生产内容恢复。

后端分钟报价实际未启用，不能以这次 UI 修改宣称价格已实时；列表来源 SQL
提速与模型吞吐分别记录在后端 `WATCHLIST-SOURCE-READ-2026-09-10.md`。


## 17:10–17:17 UTC：正式发布与回查

PR #17 合并为 `de788804e76fc0083b79d7a02755f0667009667a`，精确 main
[CI 34506216576](https://github.com/ssurmic/ducky-site/actions/runs/34506216576) 成功。
使用原 Pages 发布流程上线 `020b808c`，模块图 `35efec1b78480422015b`。
两个 JS 与带版本号的实际 CSS URL 逐字节等于构建产物，见[公开资产回执](visible-analysis-clock-assets.json)。
回滚前端为 `a3c03e39` / Pages `008ba58b`；没有 API、账户或后台队列写入。

正式中文和英文自选股保持43只、列表默认、概览第二；无需展开即见上一版日期。
待就绪股票保留真实状态和直接导图。英文切换曾再次出现摘要读取失败，Refresh
后恢复；浏览器没有捕获到 console error，同期服务器该路径返回200，尚未证明
浏览器端错误的具体原因，不能据此宣称公网读取问题完全修复。没有延长超时
或把失败当成缺少资料。后端当次十股可读数曾降到5/10，内容审核仍单独验收。
